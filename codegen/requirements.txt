# Pinned exactly; CI reproducibility of the notebooks depends on it. These are
# the versions validated end-to-end locally (codegen -> solver build -> solve ->
# C++ symbol binding) and recorded in reproduction/REPRODUCTION_REPORT.md.
# CasADi is deliberately 3.7.2: it is the version validated end-to-end here.
numpy==2.5.2
scipy==1.18.0
casadi==3.7.2
matplotlib==3.11.1
pandas==3.0.5
jupyter==1.1.1
nbconvert==7.17.1
nbformat==5.11.0
pytest==9.1.1
pillow==12.3.0
ipykernel==7.3.0

# acados_template ships with the acados source tree and must be installed from
# it, not from PyPI:
#   pip install -e ${ACADOS_SOURCE_DIR}/interfaces/acados_template
# Pinned tag: v0.6.0 (the workflow's ACADOS_TAG env var must agree with this).
